# Production Environment Configuration

# -----------------------------------------------------------------------------
# BACKEND API CONFIGURATION
# -----------------------------------------------------------------------------
# Set this to the URL where your Node.js server (from the /server directory) is deployed.
# Example: https://api.gamesup-platform.com
VITE_API_URL=https://api.your-production-domain.com

# Keep this true if you are using the custom Node.js server instead of Supabase Edge Functions
VITE_USE_LOCAL_SERVER=true

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Get these from your Supabase Project Settings -> API
VITE_SUPABASE_PROJECT_ID=your_production_project_id
VITE_SUPABASE_ANON_KEY=your_production_anon_key

# Function name if using Supabase Edge Functions (optional if using Node server)
VITE_SUPABASE_FUNCTION_NAME=make-server-production
